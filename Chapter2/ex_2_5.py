""" 以下のコードの実行結果を再現するには、PyTorch をインストールした後、
    システムのコマンドラインで `python` を実行して対話モードに入り、>>> プロンプトで次のコードを入力してください。
    （# 以降はコメントのため無視して構いません）
"""
import numpy as np # NumPy をインポート
import torch # PyTorch をインポート

torch.rand(3,3) # 3×3 行列（一様分布 [0,1)）を生成
torch.randn(2,3,4) # 2×3×4 テンソル（標準正規分布）を生成
torch.zeros(2,2,2) # 2×2×2 テンソル（全要素 0）を生成
torch.ones(1,2,3) # 1×2×3 テンソル（全要素 1）を生成
torch.eye(3) # 3×3 の単位行列を生成
torch.randint(0, 10, (3,3)) # 0（含む）〜10（含まない）の一様整数 3×3 行列を生成
